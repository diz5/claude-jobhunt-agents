# ── Personal / local-only (NEVER commit) ─────────────────────────────
# The tooling reads these locally; the public repo only ships generic templates.
# Anything named *.local.md is private (profile, ideas, notes, roadmaps-in-progress).
*.local.md
.secrets.local

# Job-search data
job-scoreboard.md
job-scoreboard.md.bak*
job-analyses/
application-kit.md
.board-state.json
.status-history.json
interview-prep/

# Resumes & docs (any format — resume drafts in markdown included)
*.pdf
*.docx
*.doc
*[Rr]esume*
old-resume/

# Local settings & session logs (contain paths + researched companies)
.claude/settings.local.json
session-helper/session-info.md

# pipeline-chart skill (has baked-in personal numbers; add back sanitized later)
.claude/skills/pipeline-chart/

# ── OS / scratch ─────────────────────────────────────────────────────
__pycache__/
*.pyc
.DS_Store
*.tmp
*.tmp.*
scratch/
