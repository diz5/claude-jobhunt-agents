---
description: Show this project's slash commands and skill entry points, with a one-line usage hint for each
---

Run `python3 .claude/commands/job_hunt_help.py` and relay its output VERBATIM — it prints the
finished cheat-sheet table (it scans `.claude/commands/` and skill frontmatter itself, preferring
each file's Chinese `help:` line). Do not re-summarize, reformat, or add commentary.
